# View test Protocol
- func create_debug_view(debug_index: int) -> ViewClass
- func get_actions() -> list[ViewTestAction]

# view test function
- 记得对于动画做好初始化，否则一次之后初始条件就不一样了。可以参考view_tests/debug_poem_popup.gd