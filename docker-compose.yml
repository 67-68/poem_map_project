# 💀 找到这一块，直接整块删掉，别留全尸：
# services:
#   rag-app: 
#     build: .
#     ...

# 🤓☝️ 你的文件里只允许留下这个纯粹的基础设施：
version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: local_qdrant
    ports:
      - "6333:6333"
    volumes:
      - ./qdrant_storage:/qdrant/storage