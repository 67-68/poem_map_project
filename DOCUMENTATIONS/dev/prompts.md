为清流-钻营事件库创建对应的config 和 sandbox  
你需要提交一份这些场景的operator报告供我审阅和修改，确保语义对得上选项。参考这些场景完成任务。
你应该首先调换当前的清流事件库的配置到浊流，因为它写得太像浊流了。然后创建清流-钻营事件库
同时确保他们存储到了正确的地方（store to字段）  在最后委派任务给prompt engineer 让他修复prompt，然后让我运行脚本生成事件。
然后再来一次检查  最终事件的效果应该是一个事件搭配三个选项，两个被锁住，参考逢迎-清流事件库的设计。选项结果暂时只有属性变动，先不做机制。